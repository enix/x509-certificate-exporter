# Locks down the `tryEmptyPassphrase` boolean inside the `pkcs12` block.
secretsExporter:
  secretTypes:
    - type: Opaque
      key: keystore-empty.p12
      format: pkcs12
      pkcs12:
        tryEmptyPassphrase: true
