# Locks down the `rbacProxy.tls.existingSecretName` override path:
# user provides a pre-existing Secret carrying tls.crt + tls.key (e.g.
# from cert-manager) and the chart skips its self-signed generation.
rbacProxy:
  enabled: true
  tls:
    existingSecretName: my-rbac-proxy-cert
