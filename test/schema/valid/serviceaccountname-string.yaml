# `rbac.*.serviceAccountName` defaults to null in the chart and is required
# when `rbac.create=false`. Users must be able to set a string here.
rbac:
  create: false
  secretsExporter:
    serviceAccountName: my-precreated-sa
  hostPathsExporter:
    serviceAccountName: my-precreated-hostpath-sa
